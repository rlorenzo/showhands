# Site block for /etc/caddy/Caddyfile on the small-apps droplet (merge, don't
# replace: the file also serves rushcall, spotthestar, and foreshock).
# flush_interval -1 disables response buffering so the SSE live-results
# stream (/api/polls/:id/stream) is delivered immediately.
showhands.rexlorenzo.com {
	reverse_proxy localhost:3002 {
		flush_interval -1
	}
}
